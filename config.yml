baseURL: 'https://steimer.com/'
languageCode: 'de-de'
title: 'Steimer Metallbau'
canonifyURLs: true

sitemap:
  changefreq: weekly

permalinks:
  lessons: "/:year/:month/:slug/"
  projects: "/:year/:month/:slug/"
  page: "/:slug/"

markup:
  goldmark:
    renderer:
      unsafe: true
      hardWraps: true
    parser:
      block: true